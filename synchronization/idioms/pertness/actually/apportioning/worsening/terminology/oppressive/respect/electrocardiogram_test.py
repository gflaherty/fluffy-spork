
import random

def test_foo():
    assert(True)
    if random.randrange(6) > 2:
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)


def test_bar():
    assert(True)
    if random.randrange(6) > 2:
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)
        assert(True)

def test_charlie():
    assert(True)
    if random.randrange(6) > 2:
        assert(True)


